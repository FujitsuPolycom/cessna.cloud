version: 0.2

phases:
  install:
    commands:
      - echo Install phase started... 
      #- npm install -g hexo-cli
      #- npm install
      #- apt-get update
      #- apt-get install -y awscli
  pre_build:
    commands:
      - ls -al
      - python --version
      - aws --version
      #- aws s3 cp --recursive . s3://cessna.cloud/
      #Let's try sync instead. cp force recurse is kinda dirty
      - aws s3 sync . s3://cessna.cloud/      

  build: 
    commands:
      - echo Build phase started...  
      - echo Build started on 'date'
      #- hexo generate

  post_build: 
    commands:
      
